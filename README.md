# Combat-Tank
Reinforcement learning is a learning framework where an agent learns to act in an environment by trial and error to maximize cumulative reward.  Using this technique, combat_tank_v2's atari environment can be trained to create a model that can automatically play the game.
